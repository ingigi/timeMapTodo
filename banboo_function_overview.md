# banboo 機能概要

このドキュメントは、タスク管理・カレンダー統合アプリ **banboo** の機能を、収益化ビジネスモデルの調査・考察に使えるよう整理したものです。

現時点で実装済みの機能、設計思想、将来的に拡張予定の領域、収益化検討で注目すべき価値軸をできるだけ網羅的に記載します。

## 1. アプリ概要

banboo は、タスクを単なる ToDo リストとして管理するのではなく、「いつやるか」までカレンダー上に配置して管理する生産性アプリです。

一般的な ToDo アプリでは、タスク一覧と予定表が分かれているため、「やるべきことは分かっているが、実際にいつ取り組むかが決まらない」という問題が起きやすくなります。banboo はこの問題に対して、左側にタスク一覧、右側にカレンダーを配置し、タスクをドラッグアンドドロップで日付や時間に割り当てられる UX を提供します。

現在は、ブラウザ版と Windows ローカルアプリ版を中心に開発されています。Firebase によるユーザーごとのデータ同期、Google アカウントログイン、Google カレンダー予定の読み取り表示にも対応しています。

## 2. コアコンセプト

banboo の中心となる考え方は、次の 3 つです。

- 左サイドバー: 「どんなタスクがあるか」を見る場所
- カレンダー: 「いつやるか」を決める場所
- 右ノートパネル: 「タスクの中身」を書く場所

左側はタスクの種類・状態・期限・タグなどで整理する場所であり、右側は日付や時間に落とし込む場所です。タスクの抽象的な整理と、実行時間の具体化を明確に分けることで、タスク管理と時間管理を一体化しています。

## 3. 想定ユーザー

現時点で特に相性が良いと考えられるユーザー層は以下です。

- 学生
  - 課題、レポート、授業準備、研究、アルバイトなどをまとめて管理したい人
  - 締切だけでなく、実際に取り組む日や時間まで決めたい人
- 個人事業主・フリーランス
  - 案件ごとのタスク、打ち合わせ、作業時間を一元管理したい人
  - Google カレンダーの予定と作業タスクを同じ画面で見たい人
- ナレッジワーカー
  - タスク管理とメモを一体化したい人
  - Notion ほど重すぎず、カレンダー中心に行動計画を立てたい人
- 社会人の個人利用
  - 仕事、学習、生活タスクをひとつの時間軸で整理したい人

## 4. 対応プラットフォーム

### 4.1 ブラウザ版

Vite / React による Web アプリとして動作します。

主な用途:

- 開発中の確認
- どの端末でもアクセスできる Web 版のベース
- Firebase 同期の検証
- Google カレンダー連携の確認

### 4.2 Windows ローカルアプリ

Electron により Windows アプリとしてビルドできます。

主な特徴:

- `banboo.exe` とインストーラーを生成可能
- ネイティブアプリ風の独自タイトルバー
- Windows 標準メニューの非表示
- アプリ内デザインに合わせたウィンドウ操作 UI
- ローカルデータ保持と Firebase 同期
- Google ログイン時は外部ブラウザ認証を利用
- Google カレンダー接続情報の保持

### 4.3 iPhone アプリ

現時点では未実装です。

ただし、最終的な構想としては、Windows ローカルアプリと iPhone アプリの両方から同じタスクを管理できる状態を目指しています。Firebase を使っているため、将来的なモバイル対応の土台はあります。

## 5. 認証・アカウント

### 5.1 Google アカウントログイン

Firebase Authentication を使い、Google アカウントでログインできます。

特徴:

- Google ログインをメイン導線として設計
- ログイン状態を保持
- ユーザーごとにタスクデータを分離
- アカウントメニューから設定画面・サインアウトにアクセス
- Electron 版では外部ブラウザ認証を利用
- 認証が途中で止まった場合に再試行できるようタイムアウト処理あり

### 5.2 メール認証

コード上はメールアドレス・パスワードによるログイン/登録機能も存在します。

ただし、プロダクト方針としては Google アカウントログインを主導線にしています。

### 5.3 アカウントポップアップ

右上のアカウント部分を押すと、アカウント関連のメニューが表示されます。

内容:

- ログイン中のメールアドレス
- アプリ設定への導線
- サインアウト

## 6. データ保存・同期

### 6.1 Firebase / Firestore

ユーザーごとのアプリデータは Firebase / Firestore に保存されます。

保存対象:

- タスク
- タグ
- ステータス
- ビュー
- ワークフロー
- タスクメモ
- カレンダー配置情報
- サイドバー幅など一部 UI 状態

これにより、同じ Google アカウントでログインした環境間でデータを同期できます。

### 6.2 ローカル保存

Electron 版ではローカルにもデータを保持します。

用途:

- アプリ再起動時の状態復元
- Firebase 読み込み前のバックアップ
- Google カレンダー接続情報の保持
- ユーザー体験の安定化

### 6.3 ユーザー分離

異なる Google アカウントでログインした場合、タスク・ビュー・タグ・ステータス・ワークフローはユーザーごとに分離されます。

## 7. タスク管理

### 7.1 タスク作成

左サイドバーの新規タスクボタンからタスクを作成できます。

作成されたタスクは、右側のタスクノートパネルで詳細編集できます。

### 7.2 タスクの主な属性

タスクは以下の属性を持ちます。

- ID
- タイトル
- 説明
- ノートブロック
- タグ
- ステータス
- 完了状態
- 完了前ステータス
- 期限日
- カードカラー
- 配置日
- 配置開始時刻
- 配置時間
- ワークフロー由来かどうか
- ワークフロー実行キー

### 7.3 完了管理

タスクには完了チェックがあります。

挙動:

- 完了チェックを入れるとステータスが自動的に「完了」になる
- 完了チェックを外すと、完了前のステータスに戻る
- 完了タスクはタイトルに取り消し線が入り、色が薄くなる
- カレンダー上・サイドバー上の両方で完了状態が視覚的に分かる

### 7.4 ステータス

標準ステータス:

- 未着手
- 進行中
- 完了

標準ステータスは削除できません。ユーザーは独自ステータスを追加・編集・削除・並び替えできます。

ステータスの並び順は、ビューのソート条件でステータスを使う場合の順序にも利用されます。

### 7.5 タグ

タスクには複数タグを付与できます。

タグの用途:

- タスク分類
- ビューのフィルター条件
- ビューのソート条件
- ワークフローで自動生成されるタスクへの付与

タグは作成・編集・削除・並び替えできます。タグの並び順はソート順にも利用されます。

## 8. 左サイドバー

### 8.1 役割

左サイドバーは「どんなタスクを見るか」を切り替える場所です。

日付や時間でタスクを見るのは右側のカレンダーの役割であり、左サイドバーはタスクの性質・状態・期限・タグなどの切り口でタスクを整理します。

### 8.2 サイドバーの表示状態

機能:

- 開閉可能
- ホバーで一時表示
- ドラッグ中でも左端に近づけると表示
- 幅をドラッグで調整可能
- ホバー表示中でも幅調整可能
- カレンダーとの境界線をドラッグしてリサイズ可能
- 表示・非表示時にスライドアニメーション

### 8.3 配置済みタスクの扱い

左サイドバーに配置済みタスクが表示される場合があります。

その場合:

- 色を薄く表示
- ドラッグ不可
- クリックすると、そのタスクが配置されているカレンダー位置に移動

これにより、左サイドバーは「タスクを確認する場所」として使いつつ、配置済みタスクを誤って再配置しないようにしています。

## 9. ビュー機能

### 9.1 ビューの目的

ビューは、左サイドバーに表示するタスク一覧を切り替える機能です。

ビューは「どんなタスクを見るか」を定義するものであり、「いつやるか」を定義するものではありません。

### 9.2 初期ビュー

初期状態では以下のビューを用意します。

- 日付なし
  - 未配置のタスクをすべて表示するビュー
- 期限が近い
  - 今日から 3 日後までに期限があるタスクを表示するビュー
  - 期限日で昇順ソート

### 9.3 ビューのフィルター条件

現在のフィルター対象:

- 配置状態
  - 未配置
  - 配置済み
- 期限日
  - 未設定
  - 設定あり
  - 今日から N 日後まで
  - 今日から N 日後から、当日から M 日後まで
- タグ
  - 複数選択
- ステータス
  - 複数選択

配置日の具体的な日付フィルターは削除されています。理由は、左サイドバーが「どんなタスクか」を見る場所であり、「いつやるか」は右側のカレンダーで扱うべきだからです。

### 9.4 ビューのソート条件

現在のソート対象:

- 期限日
- タグ
- ステータス

配置日・配置時刻によるソートは削除されています。

### 9.5 複数条件

ビューでは複数のフィルター条件・ソート条件を組み合わせられます。

フィルター:

- 条件ブロックを複数追加可能
- AND / OR 接続に対応
- タグやステータスは複数選択可能

ソート:

- 複数のソート条件を追加可能
- 条件の並び順が優先順位になる
- 並び替え可能

### 9.6 ビュー管理

できること:

- ビュー作成
- ビュー編集
- ビュー削除
- ビュー並び替え
- ドロップダウンまたはタブ形式で切り替え

少なくとも 1 つのビューが必要なため、最後の 1 つは削除できません。

### 9.7 ビュー UI

サイドバー幅に応じて表示が変わります。

- 幅が狭い場合: ドロップダウン形式
- 幅が十分な場合: タブ形式
- 表示しきれないビュー: 「・・・」メニューに格納
- 追加ボタンからビュー作成ポップアップを開く

## 10. 月表示カレンダー

### 10.1 目的

月表示は、「どの日にやるか」をざっくり決めるための画面です。

### 10.2 主な機能

- 月単位のカレンダー表示
- 日曜始まり
- 月移動
- 今日表示
- タスクを日付セルにドラッグアンドドロップで配置
- タスクカードの折り返し表示
- セル内のカード数に応じた縦幅調整
- 完了チェック
- 期限日の視覚エフェクト
- 今日表示

### 10.3 Google カレンダー予定の扱い

月表示では Google カレンダー予定を表示しません。

理由:

- 月表示は banboo タスクを日付単位で配置するための画面
- Google カレンダー予定を大量に表示するとタスク配置の視認性が落ちる
- Google カレンダー予定は週表示で時間軸と一緒に見る方が自然

## 11. 週表示カレンダー

### 11.1 目的

週表示は、「その日の何時にやるか」を決めるための画面です。

月表示で日付単位に割り当てたタスクを、週表示で時間単位に落とし込む UX を目指しています。

### 11.2 基本レイアウト

- 7 日分を一画面に表示
- 横スクロールなし
- 左に時間軸
- 1 時間ごとの横線
- 15 分単位で配置
- 現在時刻ライン
- 初期スクロール位置は現在時刻付近

### 11.3 未時刻タスク

日付は決まっているが時刻が決まっていないタスクは、各日の上部にコンパクトなカードとして表示されます。

特徴:

- 月表示のカードに近い見た目
- 横幅いっぱいに近い表示
- 縦に並ぶ
- 時間未配置などの余計なラベルは表示しない

### 11.4 時刻付きタスク

時刻が設定されたタスクは、時間軸上にカードとして配置されます。

表示内容:

- 完了チェック
- タイトル
- 開始時刻から終了時刻
- タスクカラー
- 完了時の取り消し線

### 11.5 ドラッグアンドドロップ

週表示では、タスクを時間軸にドラッグして配置できます。

特徴:

- 15 分単位でスナップ
- 未時刻タスクを時間軸に落とすと初期 30 分で配置
- ドラッグ中はカレンダー上に実際に配置されるカードに近いガイドを表示
- ガイドはカーソル位置に対してドラッグ開始位置を保つ
- 余計な二重のドラッグイメージは出さない
- ドラッグ中に詳細パネルが誤って開かない

### 11.6 リサイズ

配置済みタスクは上下をドラッグして時間の長さを変更できます。

特徴:

- 15 分単位で調整
- Google カレンダーに近い操作感
- リサイズ中に詳細パネルが開かない

### 11.7 重なり表示アルゴリズム

週表示では、同じ時間帯に重なるタスクや Google カレンダー予定を視覚的に破綻しないように配置します。

考え方:

1. 開始時刻が早い順、同時刻なら終了時刻が遅い順に並べる
2. 重なり合う予定をクラスター化する
3. クラスター内で左から右へ列を割り当てる
4. 可能な範囲でカード幅を広げる
5. テキストがかぶる場合のみ少し重ねる・ずらす
6. 右側に不要な余白を残さない

目的:

- 重なりを把握できる
- できるだけ広く表示する
- テキストが読める
- リサイズ操作ができる
- Google カレンダーに近い見た目にする

## 12. Google カレンダー連携

### 12.1 目的

Google カレンダー連携は、ログイン中の Google アカウントの予定を banboo 内に表示するための機能です。

banboo から Google カレンダー予定を編集する機能はありません。読み取り専用です。

### 12.2 主な機能

- ログイン中の Google アカウントのカレンダーを利用
- Google Calendar API を利用
- 週表示に予定を表示
- 月表示には表示しない
- 予定の編集・削除はしない
- Google カレンダー接続状態をアプリ設定で確認
- 再接続ボタン
- アプリ復帰時や一定間隔で予定を更新

### 12.3 更新処理

Google カレンダー予定は以下のタイミングで更新されます。

- 一定間隔
- アプリのフォーカス復帰
- ブラウザのタブ復帰
- アプリ起動時
- Google カレンダー再接続時

### 12.4 接続情報の保持

ブラウザ版・Electron 版ともに、Google カレンダー接続情報を保持する仕組みがあります。

Electron 版では、再起動後も Google カレンダー接続を復元できるようにしています。

## 13. タスクノート機能

### 13.1 目的

タスク詳細画面は、単なる設定フォームではなく、Notion や Evernote のような「タスクの中にあるノートページ」として設計されています。

### 13.2 右サイドパネル

タスクをクリックすると、右側からノートパネルが開きます。

特徴:

- 右からスライド表示
- 画面中央まで広めに開く
- 閉じるアイコン
- 削除アイコン
- タイトル編集
- 完了チェック
- 期限・時間・タグ・ステータス編集
- メモ編集

### 13.3 ノートエディタ

タスクごとにブロック型メモを持てます。

対応ブロック:

- 通常テキスト
- 見出し 1
- 見出し 2
- 見出し 3
- 箇条書きリスト
- 番号付きリスト
- チェックリスト
- 引用
- コードブロック

### 13.4 入力補助

対応している入力操作:

- `/` コマンドでブロックタイプ選択
- `/todo` でチェックリスト作成
- `- ` や `・` による箇条書き変換
- `1. ` による番号付きリスト変換
- Enter でリスト継続
- Tab / Shift+Tab でインデント変更
- 番号付きリストの階層番号表示
- ドラッグによるブロック並び替え
- ブロックをまたいだテキスト選択
- 選択したテキストの Backspace 削除
- 長い URL や連続文字列の自動折り返し

### 13.5 保存

メモはタスクデータの一部として保存され、Firebase 同期対象になります。

## 14. ワークフロー機能

### 14.1 目的

ワークフローは、定期的に発生するタスクを自動生成する機能です。

例:

- 毎週月曜日に「週次レビュー」を追加
- 毎週火曜日に「授業課題」を追加
- 毎月 1 日に「月次確認」を追加

### 14.2 ワークフロー設定項目

ワークフローで設定できる内容:

- ワークフロー名
- 作成されるタスク名
- 説明
- タグ
- カードカラー
- 頻度
  - 毎日
  - 毎週
  - 毎月
- 開始日
- 曜日
- 月の日付
- 期限オフセット
  - 作成日当日
  - 1 日後
  - 3 日後
  - 7 日後
  - 任意の日数

### 14.3 ワークフロー管理

できること:

- 作成
- 編集
- 一時停止
- 再開
- 削除

ワークフロー由来のタスクは、重複生成を防ぐために実行キーを持ちます。

## 15. ワークスペース設定

ワークスペース設定は、タスク管理そのものに関わる設定です。

含まれるもの:

- ワークフロー
- タグ
- ステータス

これは右上アカウントメニューから入るアプリ設定とは別物です。

将来的にプロジェクト機能を追加する場合、プロジェクトごとのタグ・ステータス・ビュー・ワークフロー管理へ拡張できる余地があります。

## 16. アプリ設定

アカウントメニューからアプリ設定画面に移動できます。

アプリ設定は、画面全体を切り替えて表示します。

現在の設定カテゴリ:

- アカウント
- Google カレンダー
- 表示
- データと同期
- アプリ情報

アプリ設定では、ワークフロー・タグ・ステータスのようなタスク管理固有の設定は扱いません。

## 17. UI / デザイン

### 17.1 基本方針

海外の仕事効率化ツールのような、落ち着いたダーク UI を目指しています。

影響を受けている方向性:

- Notion
- Linear
- Google Calendar
- モダンな SaaS / productivity tool

### 17.2 カラー

ブランドカラー:

- `#60B964`

用途:

- 主要ボタン
- 選択状態
- アクセント
- ブランドアイコン
- 一部ステータス表示

### 17.3 ロゴ・アイコン

アプリ名:

- banboo

アイコン:

- ユーザー作成の PNG アイコンを利用
- Electron アプリのアイコンにも利用
- Web アプリアイコンにも利用

### 17.4 レイアウト

メイン画面:

- 上部ヘッダー
- 左サイドバー
- 中央/右カレンダー
- 右ノートパネル

特徴:

- カードの角丸は控えめ
- 線と余白で構造を作る
- 装飾より視認性を重視
- ダークモード固定
- ライト/ダーク切り替えは現在なし

## 18. 技術構成

### 18.1 フロントエンド

- React 19
- Vite
- Tailwind CSS 系のユーティリティ
- lucide-react

### 18.2 デスクトップ

- Electron
- electron-builder
- Windows NSIS インストーラー

### 18.3 バックエンド / データ

- Firebase Authentication
- Firebase Firestore
- Google Calendar API

### 18.4 主な実行形式

- ブラウザ版
- Electron Windows アプリ
- Windows インストーラー

## 19. 現在の未実装・制約

現時点で未実装、または限定的な領域:

- iPhone アプリ
- Android アプリ
- プロジェクト機能
- チーム共有
- 複数人での共同編集
- 課金機能
- 通知・リマインダーの本格実装
- AI によるスケジュール提案
- AI によるタスク分解
- Google カレンダー予定の編集
- Google カレンダーへの書き込み
- 複数 Google カレンダーの選択
- モバイル最適化
- チーム向け管理画面
- 権限管理
- 監査ログ

## 20. 収益化検討で注目すべき価値軸

banboo の価値は、単なる ToDo 管理ではなく、「タスクを実行時間へ落とし込むこと」にあります。

収益化を考える際に重要になりそうな価値軸:

- タスク一覧とカレンダーを同時に扱える
- Google カレンダー予定を見ながらタスクを配置できる
- 月表示で日付単位、週表示で時間単位に計画できる
- タスクの中にメモを持てる
- 定期タスクをワークフローで自動生成できる
- Firebase 同期により複数端末で利用できる
- Windows ローカルアプリとして常用しやすい
- 将来的に iPhone アプリへ展開できる
- 学生、個人事業主、フリーランス、ナレッジワーカーに刺さりやすい

## 21. 収益化モデル検討のための問い

外部 AI に調査・考察してほしい問いの例:

1. banboo はどのユーザー層に最も刺さるか
2. 既存競合と比較した差別化ポイントは何か
3. 無料版と有料版の境界をどこに置くべきか
4. 個人課金、学生課金、チーム課金のどれが現実的か
5. Google カレンダー連携は有料機能にすべきか
6. ワークフロー数やビュー数の制限によるフリーミアムは成立するか
7. Windows ローカルアプリから始める戦略は妥当か
8. iPhone アプリ追加後の課金モデルはどうすべきか
9. AI スケジューリング機能を追加した場合、有料価値は高いか
10. 日本市場と海外市場で訴求を変えるべきか
11. 学生向け低価格プランは有効か
12. フリーランス向けプロプランは成立するか
13. 将来的なチーム版・プロジェクト版に拡張できるか
14. 最初の MVP 段階で課金実験するなら、どの機能を有料化すべきか

## 22. 競合比較で見てほしい領域

比較対象になりそうなサービス:

- Google Calendar
- Notion Calendar
- Todoist
- TickTick
- Sunsama
- Akiflow
- Motion
- Linear
- Notion
- Things
- Microsoft To Do

比較観点:

- タスクとカレンダーの統合度
- Google カレンダー連携の深さ
- タスクメモの強さ
- ワークフロー自動化
- 価格帯
- 個人向けかチーム向けか
- 学生利用との相性
- Windows ローカルアプリの有無
- モバイルアプリの有無
- AI 機能の有無

## 23. 現時点のプロダクトポジション仮説

banboo は、現時点では以下のようなポジションに近いです。

- Notion ほど汎用的ではない
- Google Calendar ほど予定管理専用ではない
- Todoist よりも時間配置に強い
- Sunsama / Akiflow / Motion よりも、個人が軽く使える方向に寄せられる
- 学生や個人作業者向けに、タスク・メモ・時間配置を一体化する余地がある

収益化を考える場合、最初は「個人の時間管理を本気で改善するアプリ」として小さく尖らせ、その後に iPhone アプリ、AI、プロジェクト、チーム機能へ広げる戦略が考えられます。
